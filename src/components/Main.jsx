//import WatchedBox from "./WatchedBox";
//import ListBox from "./ListBox";

const Main = ({ children }) => {
  return (
    <main className="main">
      {/* <ListBox movies={movies} /> */}
      {children}
    </main>
  );
};

export default Main;
